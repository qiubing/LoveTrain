<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="cn.nubia.activity.admin.ManagerScoreUserDetailActivity">

    <RelativeLayout
        android:id="@+id/manager_score_user_detail_layout"
        android:layout_width="fill_parent"
        android:layout_height="35.0dip">

        <TextView
            android:id="@+id/manager_score_user_detail_title"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_centerVertical="true"
            android:gravity="center"
            android:singleLine="true"
            android:text="@string/title_activity_manager_score"
            android:textColor="@color/grayslate"
            android:textSize="22.0sp" />
    </RelativeLayout>

    <ListView
        android:layout_below="@id/manager_score_user_detail_layout"
        android:id="@+id/manager_score_user_detail_listview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"></ListView>

</RelativeLayout>
