<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="cn.nubia.activity.RegistActivity">


    <EditText
        android:id="@+id/regist_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFF"
        android:hint="@string/regist_name_hint"
        android:padding="@dimen/padding"
        android:singleLine="true"
        android:textSize="@dimen/textSize16" />

    <EditText
        android:id="@+id/regist_pwd1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFF"
        android:hint="@string/regist_pwd1_hint"
        android:padding="@dimen/padding"
        android:singleLine="true"
        android:textSize="@dimen/textSize16" />
    <EditText
        android:id="@+id/regist_pwd2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFF"
        android:hint="@string/regist_pwd2_hint"
        android:padding="@dimen/padding"
        android:singleLine="true"
        android:textSize="@dimen/textSize16" />

    <TextView
        android:id="@+id/regist_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="50dp"
        android:layout_marginRight="50dp"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:paddingBottom="@dimen/marginTop"
        android:paddingTop="@dimen/marginTop"
        android:text="注　册"
        android:textSize="@dimen/textSize18" />

</LinearLayout>
